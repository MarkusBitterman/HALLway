# Ignore build outputs from performing a nix-build or `nix build` command
result
result-*

# Ignore install logs
*.log
install-history.log

# Ignore automatically generated direnv output
.direnv

# Local direnv configuration (created by user for automatic shell activation)
.envrc

# Nix flake lock (tracked, but ignore local modifications)
# flake.lock is intentionally tracked

# Editor artifacts
*.swp
*.swo
*~
.idea/
*.sublime-project
*.sublime-workspace

# OS files
.DS_Store
Thumbs.db

# ═══════════════════════════════════════════════════════════════════════════
# ROOT-LEVEL HOST CONFIGS (obsolete - use hosts/<hostname>/ instead)
# ═══════════════════════════════════════════════════════════════════════════

/configuration.nix
/hardware-configuration.nix
/secrets.nix
/home/

# Local environment overrides
.env
.env.local

# ═══════════════════════════════════════════════════════════════════════════
# SECRETS - CRITICAL: Never commit plaintext secrets
# ═══════════════════════════════════════════════════════════════════════════

# Encrypted secrets (.age files) - these are intentionally tracked
# *.age

# Plaintext secrets - NEVER commit these
hosts/*/secrets/*.key
hosts/*/secrets/*.pem
hosts/*/secrets/*.txt
hosts/*/secrets/*.pub
hosts/*/secrets/*_unencrypted

# agenix decrypted runtime secrets (if manually decrypted for inspection)
*.age.dec
*_decrypted
